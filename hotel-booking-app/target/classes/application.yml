server:
  port: 5050
  
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/hotel_booking_app
    driver-class-name: 
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
      file-size-threshold: 2MB
      
      